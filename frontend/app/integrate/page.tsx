import { ProtocolShell, EmptyState } from "@/components/protocol-shell";
export default function IntegratePage() { return <ProtocolShell><EmptyState title="Integrate PAVEL" body="Use the typed Core/Vault client and same-ID transaction persistence abstraction. Server wallets and database-authoritative state are intentionally unsupported." /></ProtocolShell>; }
