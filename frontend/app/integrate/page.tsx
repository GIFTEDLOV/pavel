import { ProtocolShell } from "@/components/protocol-shell";
import { RouteIntro, Section, TrustBoundary } from "@/components/protocol-ui";

export default function IntegratePage() { return <ProtocolShell><RouteIntro eyebrow="INTEGRATE / CLIENT BOUNDARY" title="Integrate with the chain, not a shadow database." body="Use the typed client, explicit network guard, same-ID transaction persistence, and Core/Vault read helpers. A server wallet and database-authoritative balance are intentionally unsupported." /><Section title="Integration sequence"><div className="steps"><div className="step"><h3>1. Guard</h3><p>Require Studionet chain 61999 and the canonical RPC.</p></div><div className="step"><h3>2. Precondition</h3><p>Read Core/Vault state before every write.</p></div><div className="step"><h3>3. Broadcast once</h3><p>Persist the transaction ID immediately after return.</p></div><div className="step"><h3>4. Reconcile</h3><p>Track the same ID through finality, execution, and readback.</p></div></div></Section><TrustBoundary title="Consumer responsibility" body="Semantic approval does not select economics. The Vault independently checks recipient, amount, mandate status, budgets, and replay protection." /></ProtocolShell>; }
