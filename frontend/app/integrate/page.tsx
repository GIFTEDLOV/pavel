import { ProtocolShell } from "@/components/protocol-shell";
import { PageHeader, Section, TrustBoundary } from "@/components/protocol-ui";

export default function IntegratePage() { return <ProtocolShell><PageHeader eyebrow="DEVELOPERS / CLIENT BOUNDARY" title="Integrate with the chain, not a shadow database." body="The public client can read PAVEL without a signer. Wallet writes use the same GenLayer SDK family as the deployed Studionet contracts and persist transaction IDs before reconciliation." /><Section title="Integration sequence"><div className="steps"><div className="step"><span className="step-number">01</span><h3>Guard</h3><p>Require Studionet chain 61999 and the canonical RPC.</p></div><div className="step"><span className="step-number">02</span><h3>Read latest-final</h3><p>Use account-free public reads for durable state.</p></div><div className="step"><span className="step-number">03</span><h3>Broadcast once</h3><p>Persist the returned ID immediately from the wallet.</p></div><div className="step"><span className="step-number">04</span><h3>Reconcile</h3><p>Track status, execution result, and application readback separately.</p></div></div></Section><TrustBoundary title="No server signer" body="This frontend never asks a server to hold wallet authority. Signing remains in the connected EIP-1193 wallet, and Core/Vault remain the source of truth." /></ProtocolShell>; }
