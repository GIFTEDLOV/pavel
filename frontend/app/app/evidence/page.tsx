import { ProtocolShell, EmptyState } from "@/components/protocol-shell";
export default function EvidencePage() { return <ProtocolShell><EmptyState title="Evidence" body="Definitions, HTTPS capture outcomes, content digests, byte lengths, and immutable snapshot ancestry will be shown here." /></ProtocolShell>; }
