import { ProtocolShell } from "@/components/protocol-shell";
import { NotConnected, RouteIntro, Section, TrustBoundary } from "@/components/protocol-ui";

export default function EvidencePage() { return <ProtocolShell><RouteIntro eyebrow="EVIDENCE / IDENTITY VS TRANSPORT" title="A URL is a transport, never an identity." body="PAVEL binds evidence to a Mandate, Intent, kind, authority constraint, committed digest, byte length, policy fingerprint, and evidence-set identity before semantic review." /><Section title="Evidence record"><div className="grid"><div className="card"><span className="label">Identity</span><strong className="data-value">Committed digest</strong><span className="mono">evidence ID + SHA-256 + length</span></div><div className="card"><span className="label">Transport</span><strong className="data-value">HTTPS source</strong><span className="mono">Original or approved recovery URL</span></div><div className="card"><span className="label">Snapshot</span><strong className="data-value">Write-once</strong><span className="mono">Frozen content and provenance</span></div></div></Section><TrustBoundary title="Recovery rule" body="A mirror may restore availability only when bytes, length, authority, policy, Intent, Mandate, and evidence identity all match. Different bytes are rejected." /><NotConnected body="Connect a wallet to read authenticated snapshots and recovery history from PavelCore." /></ProtocolShell>; }
