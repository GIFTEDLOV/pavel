import Link from "next/link";
import { ProtocolShell } from "@/components/protocol-shell";
import { PageHeader, Section, TrustBoundary } from "@/components/protocol-ui";

const invariants = ["Unassessed is never authorization.", "Evidence transport cannot redefine its committed identity.", "Semantic vectors cannot choose addresses, amounts, or deadlines.", "Only authenticated qualifying challenges block settlement.", "A challenge result resolves only that challenge.", "Vault accounting remains the economic source of truth.", "A missing poll response never triggers a blind rebroadcast."];

export default function SecurityPage() { return <ProtocolShell><PageHeader eyebrow="SECURITY / BOUNDARIES" title="Uncertainty is visible at every boundary." body="PAVEL separates deterministic policy and custody facts from bounded semantic review. The UI follows those same trust boundaries." /><Section title="Protocol invariants"><div className="surface list">{invariants.map((item, index) => <div className="list-row" key={item}><span className="mono">{String(index + 1).padStart(2, "0")}</span><span>{item}</span></div>)}</div></Section><TrustBoundary title="Known external boundary" tone="warning" body="A release or refund request can be canonical while the external native GEN transfer remains unconfirmed. PAVEL never collapses those states into Paid." /><Link className="text-link" href="/app/proof">Inspect deployment proof →</Link></ProtocolShell>; }
