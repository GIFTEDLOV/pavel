import { ProtocolShell, EmptyState } from "@/components/protocol-shell";
export default function IntentsPage() { return <ProtocolShell><EmptyState title="Intents" body="Frozen transaction proposals, authorization outcomes, fulfillment, and settlement directions are read from Core and Vault." /></ProtocolShell>; }
