import { ProtocolShell } from "@/components/protocol-shell";
import { NotConnected, RouteIntro, Section, TrustBoundary } from "@/components/protocol-ui";

export default async function IntentDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <ProtocolShell><RouteIntro eyebrow="INTENT / CANONICAL DETAIL" title={id} body="This view is keyed by the URL only; the record itself must be read from PavelCore and Vault." /><TrustBoundary title="No inferred success" body="Evidence-ready, authorization-pending, and reservation-pending states remain visibly distinct until the corresponding canonical read proves the transition." /><Section title="Lifecycle columns"><div className="steps"><div className="step"><h3>Mandate and proposal</h3><p>Frozen identity, terms, amount, recipient, and policy fingerprint.</p></div><div className="step"><h3>Evidence and consensus</h3><p>Snapshot identity plus bounded authorization or fulfillment vector.</p></div><div className="step"><h3>Vault reservation</h3><p>Exact amount and recipient rechecked synchronously by the Vault.</p></div><div className="step"><h3>Settlement observation</h3><p>Release/refund pending remains distinct from recipient confirmation.</p></div></div></Section><NotConnected body="Connect a wallet to read this Intent. A path segment is not a cached record." /></ProtocolShell>; }
