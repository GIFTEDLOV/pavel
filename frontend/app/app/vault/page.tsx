import { ProtocolShell, EmptyState } from "@/components/protocol-shell";
export default function VaultPage() { return <ProtocolShell><EmptyState title="Vault" body="PavelVault is the economic source of truth. Available, reserved, pending, recovered, and conservation state are never inferred from Core." /></ProtocolShell>; }
