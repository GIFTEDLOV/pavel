import { ProtocolShell } from "@/components/protocol-shell";
import { AddressValue, DataCard, NotConnected, RouteIntro, Section, TrustBoundary } from "@/components/protocol-ui";
import { configuredContracts } from "@/lib/pavel/config";
import { PAVEL_NETWORK } from "@/lib/pavel/network";

export default function ProofPage() { const contracts = configuredContracts(); return <ProtocolShell><RouteIntro eyebrow="PROOF / PROVENANCE" title="Trace source to state without overclaiming deployment." body="This page surfaces configuration and proof concepts. It does not invent live addresses or promote qualification artifacts to canonical production." /><Section title="Network configuration"><div className="grid"><DataCard label="Network" value={PAVEL_NETWORK.alias} note={`chain ${PAVEL_NETWORK.chainId}`} /><DataCard label="RPC" value="Studionet API" note={PAVEL_NETWORK.rpcUrl} /><DataCard label="Currency" value={PAVEL_NETWORK.currency} note="Native GEN" /></div></Section><Section title="Configured contracts"><div className="card"><AddressValue label="Core" value={contracts.core} /><AddressValue label="Vault" value={contracts.vault} /></div></Section><TrustBoundary title="Qualification boundary" body="The corrected qualification-v2 Core is source-verified in local qualification artifacts. It is not treated as a canonical production address by this UI." /><NotConnected body="Connect a wallet to reconcile deployed code, contract schema, transactions, and state readback against the configured pair." /></ProtocolShell>; }
