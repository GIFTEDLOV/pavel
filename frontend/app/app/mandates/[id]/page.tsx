import { ProtocolShell } from "@/components/protocol-shell";
import { NotConnected, RouteIntro, Section, TrustBoundary } from "@/components/protocol-ui";

export default async function MandateDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <ProtocolShell><RouteIntro eyebrow="MANDATE / SEALED POLICY" title={id} body="The route identifies a read target; only the Core record can establish its status, fingerprint, ancestry, or limits." /><Section title="Policy fields"><div className="grid"><div className="card"><span className="label">Constitution</span><strong className="data-value">Frozen text</strong><span className="mono">Purpose and boundaries</span></div><div className="card"><span className="label">Economic limits</span><strong className="data-value">Deterministic</strong><span className="mono">Single, epoch, total budgets</span></div><div className="card"><span className="label">Evidence policy</span><strong className="data-value">Authority-bound</strong><span className="mono">Origins sealed with policy</span></div></div></Section><TrustBoundary title="Fingerprint before trust" body="The definition hash is displayed only after a canonical read. Text rendered by this page is explanatory and does not stand in for the Mandate." /><NotConnected body="Connect a wallet to load the sealed Mandate record and delegation ancestry." /></ProtocolShell>; }
