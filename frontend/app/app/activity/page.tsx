import { ProtocolShell } from "@/components/protocol-shell";
import { NotConnected, RouteIntro, Section, TrustBoundary } from "@/components/protocol-ui";

export default function ActivityPage() { return <ProtocolShell><RouteIntro eyebrow="ACTIVITY / SAME-ID TRACKING" title="A transaction is a trace, not a spinner." body="PAVEL persists a returned GenLayer transaction ID immediately and reconciles that same ID through consensus, finalization, execution result, and authoritative state readback." /><TrustBoundary title="No rebroadcast" body="A missing poll response is ambiguous. The client never treats it as permission to submit a replacement transaction." /><Section title="Observed stages"><div className="steps"><div className="step"><h3>Transaction ID received</h3><p>Submission is recorded locally before polling begins.</p></div><div className="step"><h3>Accepted / finalizing</h3><p>Consensus status is provisional and is not execution success.</p></div><div className="step"><h3>Finalized execution result</h3><p>Finality and successful execution are checked separately.</p></div><div className="step"><h3>Readback</h3><p>The UI confirms protocol state rather than trusting a toast message.</p></div></div></Section><NotConnected body="Connect a wallet to reconcile persisted transaction IDs against the canonical network." /></ProtocolShell>; }
