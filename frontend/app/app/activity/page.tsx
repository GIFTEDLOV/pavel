import { ProtocolShell, EmptyState } from "@/components/protocol-shell";
export default function ActivityPage() { return <ProtocolShell><EmptyState title="Activity" body="Transaction history will distinguish submission, consensus, finalization, execution success, ambiguity, and readback." /></ProtocolShell>; }
