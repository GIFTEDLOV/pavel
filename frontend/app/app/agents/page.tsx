import { ProtocolShell } from "@/components/protocol-shell";
import { NotConnected, RouteIntro, Section, TrustBoundary } from "@/components/protocol-ui";

export default function AgentsPage() { return <ProtocolShell><RouteIntro eyebrow="AGENTS / DELEGATION" title="Agents receive bounded authority, not a withdrawal key." body="Agent registrations and delegation ancestry are Core state. A child Mandate can narrow deterministic limits and policy requirements but cannot expand its parent." /><TrustBoundary title="Deterministic boundary" body="Addresses, principals, limits, deadlines, Mandate IDs, and recipients come from frozen state. Semantic review cannot invent them." /><Section title="Agent review surface"><div className="grid"><div className="card"><span className="label">Identity</span><strong className="data-value">Address-bound</strong><span className="mono">No URL-defined identity</span></div><div className="card"><span className="label">Delegation</span><strong className="data-value">Narrowing only</strong><span className="mono">Numeric bounds + semantic vector</span></div><div className="card"><span className="label">Authority</span><strong className="data-value">Explicit</strong><span className="mono">Registered in PavelCore</span></div></div></Section><NotConnected body="Connect a wallet to read registered agents and delegation ancestry from PavelCore." /></ProtocolShell>; }
