import { ProtocolShell, EmptyState } from "@/components/protocol-shell";
export default function DisputesPage() { return <ProtocolShell><EmptyState title="Disputes" body="Challenges append evidence and preserve the original authorization, reservation, and fulfillment records." /></ProtocolShell>; }
