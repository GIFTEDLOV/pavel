import { ProtocolShell } from "@/components/protocol-shell";
import { NotConnected, RouteIntro, Section, StatusBadge, TrustBoundary } from "@/components/protocol-ui";

export default function DisputesPage() { return <ProtocolShell><RouteIntro eyebrow="DISPUTES / OPEN CHALLENGES" title="Adverse evidence is append-only and independently assessed." body="Any properly formed challenger can introduce evidence during the challenge window. Submission alone is not a settlement block: only a deterministically admissible, authenticated challenge qualifies." /><div className="grid"><div className="card"><span className="label">Submitted</span><StatusBadge status="CHALLENGE_SUBMITTED" /><span className="mono">Recorded, not yet qualifying</span></div><div className="card"><span className="label">Qualifying</span><StatusBadge status="CHALLENGE_QUALIFYING" /><span className="mono">Blocks settlement</span></div><div className="card"><span className="label">Resolution</span><StatusBadge status="CHALLENGE_RESOLVED" /><span className="mono">Resolves only itself</span></div></div><TrustBoundary title="Challenge-set integrity" body="Challenge IDs, evidence snapshots, deadlines, and outcomes are independent. The UI must never collapse the first open challenge into the whole set." /><Section title="Deterministic processing"><div className="steps"><div className="step"><h3>Admit</h3><p>Exact Intent, authority, evidence identity, uniqueness, and capacity checks.</p></div><div className="step"><h3>Freeze</h3><p>Challenge evidence and snapshot identity become immutable.</p></div><div className="step"><h3>Assess</h3><p>Bounded semantic output is attached to this challenge only.</p></div><div className="step"><h3>Settle</h3><p>Settlement remains blocked while any qualifying unresolved challenge remains.</p></div></div></Section><NotConnected body="Connect a wallet to inspect canonical challenge indexes and status histories." /></ProtocolShell>; }
